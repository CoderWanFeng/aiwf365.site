# 必填：远端服务器地址 (IP 或域名)
TENCENT_SERVER_HOST=

# 可选：以下都有默认值
TENCENT_SERVER_USER=root
TENCENT_SERVER_PORT=22
TENCENT_SSH_KEY=~/.ssh/id_rsa
REMOTE_DIR=/opt/website/aiwf365/aiwf365.site
LOCAL_DIR=./

# 可选开关
# DRY_RUN=true           只打印要执行的命令，不实际同步
# DEPLOY_VERBOSE=true    显示每个子进程的完整命令（调试用）
